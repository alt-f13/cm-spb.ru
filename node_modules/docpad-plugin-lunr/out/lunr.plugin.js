// Generated by CoffeeScript 1.11.1
(function() {
  var lunrdoc,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  lunrdoc = require('./lunrdoc');

  module.exports = function(BasePlugin) {
    var LunrPlugin;
    return LunrPlugin = (function(superClass) {
      extend(LunrPlugin, superClass);

      function LunrPlugin() {
        return LunrPlugin.__super__.constructor.apply(this, arguments);
      }

      LunrPlugin.prototype.name = 'lunr';

      LunrPlugin.prototype.extendTemplateData = function(arg) {
        var templateData;
        templateData = arg.templateData;
        lunrdoc.init(this.docpad);
        templateData.getLunrSearchPage = function(index, placeholder) {
          return lunrdoc.getLunrSearchPage(index, placeholder);
        };
        return templateData.getLunrSearchBlock = function(searchPage, placeholder, submit) {
          return lunrdoc.getLunrSearchBlock(searchPage, placeholder, submit);
        };
      };

      LunrPlugin.prototype.writeAfter = function(opts) {
        var _indexDocument, docpad, index, indexName, ref;
        docpad = this.docpad;
        _indexDocument = function(docpad, collection) {
          var indexCollection;
          indexCollection = docpad.getCollection(collection);
          if (indexCollection) {
            indexCollection.forEach(function(document) {
              return lunrdoc.index(indexName, document);
            });
          }
        };
        if (this.config.indexes) {
          ref = this.config.indexes;
          for (indexName in ref) {
            index = ref[indexName];
            if (Array.isArray(index.collection)) {
              index.collection.forEach(function(collection) {
                _indexDocument(docpad, collection);
              });
            } else {
              _indexDocument(docpad, index.collection);
            }
          }
          return lunrdoc.save();
        }
      };

      return LunrPlugin;

    })(BasePlugin);
  };

}).call(this);
